const ShowBloodLists = () => {
  // const { customState } = useMyContext();
  // console.log(customState);

  return (
    <div className="h-screen">
      <div className="mt-20">
        <h1 className="titleStyle">Blood list </h1>
      </div>
    </div>
  );
};

export default ShowBloodLists;
